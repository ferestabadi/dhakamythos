PUBLIC_SANITY_PROJECT_ID=
PUBLIC_SANITY_DATASET=production
# PUBLIC_SITE_URL= # set when a custom domain replaces *.vercel.app

# publish loop (Vercel project settings, not local):
# SANITY_REVALIDATE_SECRET=  # shared with the Sanity webhook
# VERCEL_DEPLOY_HOOK_URL=    # Vercel → Settings → Git → Deploy Hooks
