<svelte:head>
	<title>Index — DHAKAMYTHOS</title>
	<meta name="description" content="All works, year by year." />
</svelte:head>

<!-- Stub — the Index view (rows + tag filters) lands in M3. -->
<div class="page">
	<h1 class="type-display">Index</h1>
	<p class="type-meta note">First works arriving soon</p>
</div>

<style>
	.page {
		padding: 0 var(--gutter) var(--space-9);
	}

	.note {
		color: var(--muted);
		margin-top: var(--space-5);
	}
</style>
