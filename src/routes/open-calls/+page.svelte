<svelte:head>
	<title>Open calls — DHAKAMYTHOS</title>
	<meta name="description" content="Calls for collaboration — submit your work." />
</svelte:head>

<!-- Stub — the calls list (status pills + deadlines) lands in M3. -->
<div class="page">
	<h1 class="type-display">Open calls</h1>
	<p class="type-meta note">No calls open right now</p>
</div>

<style>
	.page {
		padding: 0 var(--gutter) var(--space-9);
	}

	.note {
		color: var(--muted);
		margin-top: var(--space-5);
	}
</style>
