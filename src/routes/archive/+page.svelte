<svelte:head>
	<title>Archive — DHAKAMYTHOS</title>
	<meta name="description" content="Scraps, sketches and process — the wall." />
</svelte:head>

<!-- Stub — the Wall (scrap grid + lightbox) lands in M3. -->
<div class="page">
	<h1 class="type-display">Archive</h1>
	<p class="type-meta note">First scraps arriving soon</p>
</div>

<style>
	.page {
		padding: 0 var(--gutter) var(--space-9);
	}

	.note {
		color: var(--muted);
		margin-top: var(--space-5);
	}
</style>
